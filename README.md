# 📚 Books to Scrape - Web Scraper

A simple Python project that scrapes book data (title, price, rating, and cover image) from the website [books.toscrape.com](https://books.toscrape.com) and saves the results into a CSV file.

## 🔍 Features

- Extracts:
  - Book title
  - Price
  - Rating
  - Cover image URL
- Saves all results in `books.csv`

## 🧰 Technologies Used

- Python 3
- Requests
- BeautifulSoup (with `lxml` parser)
- CSV module (built-in)

## 🚀 How to Use

1. 📦 Install the required libraries (if not already installed):

   ```bash
   pip install requests beautifulsoup4 lxml
2.▶️ Run the script:

python book_scaper.py

📁 Check the generated file books.csv in the same folder.
